#  _                      _            _    
# | |__  _   _ _ __  _ __| | ___   ___| | __
# | '_ \| | | | '_ \| '__| |/ _ \ / __| |/ /
# | | | | |_| | |_) | |  | | (_) | (__|   < 
# |_| |_|\__, | .__/|_|  |_|\___/ \___|_|\_\
#        |___/|_|                           
# 

general {
    ignore_empty_input = true
}

background {
    monitor =
    #path = /home/pigges/Pictures/Wallpapers/hyprfog-2.png   # only png supported for now
    path = screenshot
    color = rgba(25, 20, 20, 1.0)

    blur_passes = 1
    blur_size = 15
    noise = 0.005
    contrast = 0.4
    brightness = 0.5
    vibrancy = 0.1696
    vibrancy_darkness = 0.1
}

input-field {
    monitor =
    size = 450, 450
    outline_thickness = 3
    dots_size = 0.33 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.15 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    dots_rounding = -1 # -1 default circle, -2 follow input-field rounding
    outer_color = rgb(99, 75, 126)
    inner_color = rgba(0, 0, 0, 0.7)
    font_color = rgb(10, 10, 10)
    fade_on_empty = false
    fade_timeout = 1000 # Milliseconds before fade_on_empty is triggered.
    #placeholder_text = <i>Input Password...</i> # Text rendered in the input box when it's empty.
    placeholder_text = 
    hide_input = true
    rounding = -1 # -1 means complete rounding (circle/oval)
    check_color = rgb(204, 136, 34)
    fail_color = rgb(204, 34, 34) # if authentication failed, changes outer_color and fail message color
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i> # can be set to empty
    fail_transition = 300 # transition time in ms between normal outer_color and fail_color
    capslock_color = 1
    numlock_color = -1
    bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
    invert_numlock = false # change color if numlock is off
    swap_font_color = false # see below
    position = 0, 0
    halign = center
    valign = center
}


label {
    monitor =
    text = cmd[update:1000] printf "%(%d.%m)T"
    color = rgba(99, 75, 126, 1.0)
    font_size = 25
    font_family = Noto Sans Bold

    position = 0, -25
    halign = center
    valign = center
    shadow_passes = 5
    shadow_size = 10
}

label {
    monitor =
    text = cmd[update:1000] echo "$TIME"
    color = rgba(200, 200, 200, 1.0)
    font_size = 75
    font_family = Noto Sans Bold
    position = 0, -10
    halign = center
    valign = center
    shadow_passes = 5
    shadow_size = 10
}

label {
    monitor =
    #text = cmd[] grep "^`whoami`:" /etc/passwd | awk -F: '{print $5}'
    #text = cmd[] echo "$(uname -n)"
    text = Activate Linux
    color = rgba(200, 200, 200, 1.0)
    font_size = 15
    font_family = Noto Sans
    position = -160, 30
    halign = right
    valign = bottom
    shadow_passes = 5
    shadow_size = 10
}

label {
    monitor =
    text = Go to Settings to activate Linux
    color = rgba(200, 200, 200, 1.0)
    font_size = 10
    font_family = Noto Sans
    position = -100, 20
    halign = right
    valign = bottom
    shadow_passes = 5
    shadow_size = 10
}

